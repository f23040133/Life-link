
// AI Service is temporarily disabled for stable deployment
export const initializeChat = () => {
  console.log("AI Chat initialized (Stubbed)");
};

export const sendMessageToAI = async (message: string): Promise<string> => {
  return "The AI Assistant is currently undergoing maintenance. Please check back later.";
};
